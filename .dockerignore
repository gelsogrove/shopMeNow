# Never ship secrets or local state into an image
.env
.env.*
!.env.example

# Heavy / local-only
.git
.worktrees
.codegraph
node_modules
**/node_modules
**/dist

# Not needed inside the runtime images
docs
pdf
openspec
outreach_scripts_esp.md
deploy
docker-compose*.yml
