{
  "name": "ecolaundry-chatbot",
  "version": "0.1.0",
  "description": "Ecolaundry chatbot — prompt-driven LLM with cached system prompt, session state, escalation by email",
  "type": "module",
  "scripts": {
    "demo": "node --import tsx agent.ts",
    "demo:contract": "node --import tsx test-contract.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.17",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
