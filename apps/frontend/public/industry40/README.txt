Drop swap-in photos here to override the built-in mockups on /industry-40:
  hero.jpg      -> article hero (falls back to /robot.png)
  chatbot.jpg   -> Custom Chatbots section (falls back to chat mockup)
  vision.jpg    -> Computer Vision section (falls back to scan mockup)
Recommended: landscape, >=1200px wide, .jpg/.webp.
